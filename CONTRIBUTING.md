# Contributing

Create a focused branch, add tests for behavior changes, run `make lint test`, and document user-visible changes in `CHANGELOG.md`. Do not commit UCI data, model weights, credentials, fabricated benchmark results, or claims that anonymous variables are physical root causes.

